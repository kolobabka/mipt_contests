#include "tree.h"

int main () {

    // Tree* tmp = NULL;
    // Tree* root = NULL;

    //root = TreeInit_t ();
    // return 0;
    //TreeInit (&mainRoot);
    TheStartMenu ();

    // root = InsertIntoTree (root, 150, 7, 10, 13);
    // root = InsertIntoTree (root, 30, 7, 10, 13);
    // root = InsertIntoTree (root, 20, 7, 10, 13);
    // root = InsertIntoTree (root, 15, 7, 10, 13);
    // root = InsertIntoTree (root, 0, 7, 10, 13);
    // root = InsertIntoTree (root, 15, 6, 12, 13);
    // root = InsertIntoTree (root, 15, 6, 14, 13);
    

    
    // for (int i = 100; i < 100000; i++)
    // {
    //     root = InsertIntoTree (root, i, 7, 10, 234);
    //     //printf ("root->key = %u\n", root->key);
    // }

    // for (int i = 100; i < 100000; i++)
    // {
    //     root = InsertIntoTree (root, i, 8, 12, 234);
    //     //printf ("root->key = %u\n", root->key);
    // }
    // // inorder (root);
    // // root = DeleteElem (root, 65630);
    // // printf ("root->key = %u\n", root->key);
    // tmp = FindElem (root, 150);
    // printf ("tmp->next = %f\n", tmp->next->value_3);
    // DeleteTree (root);
    return 0;
}